# AI Sales Assistant

넥스트라인 영업 지원 데모 앱 (Vue 3 + FastAPI). 넥스트라인 실제 데이터(`11_nextline_practice/data/`)로 시딩되며, `data/`·`docs/` 원본은 읽기만 하고 수정하지 않는다.

## 실행 방법

**백엔드**
```
cd output/ai_sales_assistant/backend
pip install -r requirements.txt
python main.py
```
최초 실행 시 자동으로 DB(`sales_assistant.db`)를 실제 데이터로 시딩한다. → http://localhost:8004

**프론트엔드**
```
cd output/ai_sales_assistant/frontend
npm install
npm run dev
```
→ http://localhost:5177

## 데모 계정
- 관리자: `admin` / `1234`
- 조직원: `sls001` ~ `sls040` / `1234` (담당 고객사는 `거래처_담당자_매핑.xlsx` 기준으로 자동 배정됨)

## 반드시 알아야 할 시뮬레이션 caveat

- **AI 기능은 전부 규칙 기반 계산**이다(`backend/ai_rules.py`). 실제 LLM을 호출하지 않으며, 모든 점수/등급은 설명 가능한 공식으로 산출된다. 나중에 실제 LLM 연동으로 바꿀 때는 이 파일 하나만 교체하면 된다.
- **고객 문의(위험도) 데이터는 실제 1:1 기록이 아니라 통계적 시뮬레이션**이다. `고객문의_CS접수_데이터.xlsx`는 B2C 소비자 CS 데이터라 60개 B2B 거래처와 연결할 키가 없다. 대신 그 데이터의 실제 분포(문의유형/VOC태그/평점 비율)를 거래처별로 거래규모에 비례해 재현했다 — 실제 개별 상담 이력이 아님을 화면/문서에 명시해야 한다.
- **미팅 메모 "AI 추출"은 키워드 매칭 시뮬레이션**이다 (실제 자연어 이해가 아님). 화면에도 "키워드 기반 시뮬레이션"이라고 표시되어 있다.
- 서비스/제품 데이터는 실제 `거래처_매입매출_데이터.xlsx` 거래 내역에서 추출했다. 추천 대상은 그중 "IT서비스" 카테고리 8종(DB관리/ERP유지보수/SW라이선스/네트워크관리/모니터링/백업서비스/보안솔루션/클라우드호스팅)으로 한정했다 — 원자재/부품을 사던 거래처에 원자재를 "추천"하는 건 의미가 없기 때문이다.

## 원칙

"AI가 영업을 대신하는 시스템"이 아니라 "고객 데이터를 담당자가 행동할 수 있는 정보로 전환하는 시스템"이다. 모든 AI 출력은 참고 정보이며, 화면 문구도 "(참고용)", "검토 후 수정하세요" 등으로 이를 명시한다.
